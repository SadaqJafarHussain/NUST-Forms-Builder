"use client";

// Language switcher is disabled - Arabic only mode
// This component returns null as there's only one language available
export function LanguageSwitcher() {
  // Hidden since we only support Arabic
  return null;
}
